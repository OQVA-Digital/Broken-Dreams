---
import Layout from '../layouts/Layout.astro';

import { Image } from "astro:assets"
import Bg from "../assets/van-bgs/home.png"
import CoffeeCup from "../assets/coffee-cup.png"

import '../styles/slide_page.css'

---

<Layout title="Broken Dreams Coffee Co.">

	<div class="bg_ctnr">
		<Image src={Bg} alt="Broken Dreams background image" class="bg reveal_bg" />
	</div>
	<section class="intro shadow_ctnr">
		<div class="main base_width">
			<div class="content">
				<h1 class="reveal_steps1">Every Sip Tells a Story</h1>

				<p class="reveal_steps1">At <strong>Broken Dreams</strong>, our mobile cafes are more than just a pit-stop for your daily caffeine fix. <b>They are the crossroads where authenticity meets aspiration.</b> We believe a cup of coffee should stir the soul, not just the senses. Every one of our baristas is an artist, a musician, an actor—a dreamer.</p>
				
				<a href="/dream-with-us" class="cta reveal_steps1">Dream with us</a>

				<a href="/contact" class="secondary reveal_steps1">Contact us</a>

				<hr class="reveal_steps1">
			</div>

			<div class="float_ctnr reveal_2">
				<Image src={CoffeeCup} alt="Coffee Cup" class="coffee_cup" draggable="false"/>
			</div>
		</div>
	</section>
</Layout>

<style>
	.intro .base_width {
		display: flex;
		flex-flow: row wrap;
	}

	.intro .base_width .content {
		width: clamp(10rem, 85vw, 66rem);
		text-align: left;
    	height: fit-content;
	}

	h1 {
    	font-size: clamp(1rem,15vw,12rem);
	}

	@keyframes float {
		0% {
			transform: translate3d(0, 0, 0);
		} 50% {
			transform: translate3d(0.2rem, -0.8rem, 0);
		} 100% {
			transform: translate3d(0, 0, 0);
		}
	}

	.float_ctnr {
		margin-inline: auto;
	}

	.coffee_cup {
		width: clamp(10rem,50vw,30rem);
		height: fit-content;
    	filter: drop-shadow(2rem -3rem 5rem var(--dark));
		animation:float 5s ease infinite alternate;
	}

	@media screen and (min-width:1000px) {
		.coffee_cup {
			margin-top: 6rem;
		}

        .icon {
            width: 1.5rem;
            height: 1.5rem;
        }
	}
</style>